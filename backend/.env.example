# ---------------------------------------------------------------------------
# data.go.kr OpenAPI service key
# ---------------------------------------------------------------------------
# Get your key from https://www.data.go.kr → 마이페이지 → 인증키
#
# Paste either the DECODED key (recommended) or the percent-encoded key;
# the backend URL-decodes it automatically.
#
# Variable name accepted:
#   DATA_GO_KR_SERVICE_KEY   ← preferred
#   DATAGOKR_API_KEY         ← legacy alias (still supported)
#
DATA_GO_KR_SERVICE_KEY=your_decoded_service_key_here

# ---------------------------------------------------------------------------
# Place search
# ---------------------------------------------------------------------------
PLACES_CSV_PATH=data/medical_places.csv
SEARCH_RADIUS_KM=3.0

# ---------------------------------------------------------------------------
# Ranking weights (must sum to 1.0)
# ---------------------------------------------------------------------------
RANKING_W_DIST=0.35
RANKING_W_OPEN=0.30
RANKING_W_TRI=0.35
